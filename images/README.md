# Images

Placeholders still to be replaced:

| File | Used by | Replace with |
|---|---|---|
| `posit-conf-2023-placeholder.svg` | slide 6 (`03-template.qmd`) | a screenshot of the 2023 talk/slide, then change the `src` |
| `dirt-data-reports-01.png` | slide 10b (`05b-app-demo.qmd`), as the video **poster** | a real screenshot of the app (first frame of the recording is ideal) |
| `dirt-data-reports.webm` / `.mp4` | slide 10b (the demo, with controls): the `<video>` sources | the recorded demo, ≤ 2:00: upload → parameters → render → download. The slide already references both names, so just add the file(s); the placeholder tag disappears on its own |
