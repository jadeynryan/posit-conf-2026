# Archived slide versions

Not rendered — Quarto skips `_`-prefixed directories. Kept for reference.

- `01-hook-rail.qmd` — the hook slides with a static footer rail driven by
  reveal's auto-animate (1930s · 1936 · 1939). Superseded by the scrolling
  timeline tape in `sections/01-hook.qmd` + `assets/timeline.js`, which uses
  the tighter dates (Aug 1930 · late 1938 · Mar 1939). Needs the `.tl*`
  rules that were in `theme/brand.scss` at that commit — see git history.
