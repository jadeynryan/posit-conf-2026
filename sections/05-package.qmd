<!-- BEAT: Layer 2 · Package · The Cookbook (2:00). Plain, practical. A published cookbook for anyone with R; end on 'not everyone is'. · OWNER: J -->
<!-- SLIDE 8 · Layer 2: the {soils} package. Published by WSDA + WSU for the public (WA data swapped
     for placeholders), not for an internal team. The WSU credit is text for now; confirm with Jadey,
     swap in a logo if she has one. -->

## Layer 2: Package [· The Cookbook]{.h2-sub} {#package}

{{< rings lit=2 class="rings--crumb" >}}

::: {.lede}
`{soils}`: An R package for coders to adapt the template and generate reports.
:::

::: {.fill .fill--center}

```{=html}
<div class="flow">
  <div class="node card card--l2">
    <code>.qmd</code>
    <p class="emphasis"><br>Modular Quarto files</p>
    <p class="muted">Customize report content.</p>
  </div>
  <div class="arrow">&plus;</div>
  <div class="node card card--l2">
    <code>.R</code>
    <p class="emphasis"><br>Functions & Scripts</p>
    <p class="muted">Validate, process, and visualize data; render reports.</p>
  </div>
    <div class="arrow">&plus;</div>
    <div class="node card card--l2">
    <code>roxygen2 & pkgdown</code>
    <p class="emphasis">Documentation</p>
    <p class="muted">Document functions and guide package use.</p>
  </div>
</div>
```

:::

::: {.notes}
*⏱ 1:00 · Jadey*

**Jadey:** 
Developing an R package to support these teams felt like a natural progression. For people with the right technical expertise, we wanted to give them flexibility to adapt the template to their projects and build their own soil health reports. 

We broke up our one big report template into smaller, modular Quarto files and replaced all the Washington-specific components with placeholders for users to swap in information applicable to their own project. 

Then we pulled a lot of the R code out of the original Quarto template and turned that code into reusable functions that handle the core work like validating, processing, and visualizing the data. We also created an R script that renders all the reports at once.

The last piece was documentation. Once the Quarto files were modularized and the code became reusable, we wanted to make sure people could understand what was available and how to use this reporting workflow. We used {roxygen2} to document all functions and {pkgdown} to create a website with tutorials.
:::

. . .

::: {.body .bridge}
[Everything you need to adapt the workflow to your own data.]{.emphasis}
:::

::: {.notes}
*⏱ 0:30 · Jadey*

**Jadey:** 
So the shift was from a reporting workflow built specifically for a Washington State project to a reusable toolkit that any R user could adapt for their own soil sampling project.

Let's take a look at how it works.
:::

<!-- SLIDE 9 · Layer 2: what a developer actually runs (Jadey to build; see the note below).
     Target: `library(soils)` plus the one call a user makes (real API still open), optionally a
     pkgdown screenshot beside it. Not the folder tree; that is slide 8. -->

## Layer 2: Package [· The Cookbook]{.h2-sub} {#package-usage}

{{< rings lit=2 class="rings--crumb" >}}

::: {.lede}
How farm advisers, who can code, unlock access to the reporting workflow.
:::

:::: {.columns}
::: {.column width="40%"}
```{=html}
<figure class="then-card">
  <img src="images/soils-pkgdown.png" alt="Screenshot of pkgdown site showing how to initiate a {soils} project with RStudio">
</figure>
```
:::

::: {.column width="5%"}
:::

::: {.column width="50%"}
```{=html}
<p class="body" style="margin-top:20px;"><span class="emphasis">After importing their data and customizing report content...</span></p>
```

```{=html}
<div class="code-window">
<div class="chrome"><span class="r"></span><span class="y"></span><span class="g"></span><span class="filename">render-reports.R</span></div>
```
```r
# Render the reports
report_df |> 
  purrr::pwalk(
    quarto::quarto_render,
    input = "farmer-report.qmd"
  )
```
```{=html}
</div>
```
:::

::::

::: {.notes}
*⏱ 0:30 · Jadey*

**Jadey:** On the left is a screenshot of the tutorial on the {pkgdown} website that shows how to use the RStudio project wizard to create a project, which sets up the {soils} directory with the modular .qmd files, example data, and R scripts. Users then continue through the other tutorials to import their data, customize the content and branding of the reports, and then use the `render-reports.R` script to generate all of their reports once. 
:::

. . .

<!-- the bridge to Layer 3, not a takeaway (the "decouple before Shiny" lesson belongs to Lessons Learned) -->
::: {.body .bridge style="margin-top:72px;"}
[Anyone comfortable in R can run their own soil survey reports.]{.emphasis} [Not everyone is.]{.muted}
:::

::: {.notes}
*⏱ 0:30 · Jadey*

**Jadey:** Now we're reaching anyone comfortable in R. But not everyone who would like to run these reports is. So how can we reach the farm advisers with zero code experience?
:::
